init:
  - git config --global core.autocrlf true
environment:  
  matrix:
    - nodejs_version: "7"
    - nodejs_version: "6"
install:  
  - ps: Install-Product node $env:nodejs_version
  - cd frontend
  - npm install
  - npm run build
test_script:
  - node --version
  - npm --version

build: off